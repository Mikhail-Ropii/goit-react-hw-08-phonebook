import styled from '@emotion/styled';
import { NavLink } from 'react-router-dom';

export const MainMenuLink = styled(NavLink)`
  margin-right: 10px;
`;

export const ContainMainMenu = styled.div`
  display: flex;
  align-items: center;
`;
